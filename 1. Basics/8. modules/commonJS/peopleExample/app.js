// Import the person object
const { person, person2 } = require('./person');

// Use the imported object
console.log(person.greet());  // Outputs: Hi, I'm Bob and I'm 30 years old.
console.log(person2.greet());  // Outputs: similar

