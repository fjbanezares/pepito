<%- include('partials/header') %>

    <h1>Here I am</h1>
    <p>
        <%= textHome %>
    </p>
    <% console.log(posts) %>

        <% for (var i=0; i < posts.length; i++) { %>
            <% console.log(posts[i].title); %>
                <% };%>
                    <%- include('partials/footer') %>