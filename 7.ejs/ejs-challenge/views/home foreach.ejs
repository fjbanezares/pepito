<%- include('partials/header') %>

    <h1>Here I am</h1>
    <p>
        <%= textHome %>
    </p>
    <% console.log(posts) %>

        <% posts.forEach(function(post) { %>
            <% console.log(post.title); %>
                <% });%>
                    <%- include('partials/footer') %>